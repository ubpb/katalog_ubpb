| {{#if can_be_rendered}}
| {{#facet}}
.facet.cat-histogram-facet
  .cat-nav-header
    | {{ t("facets." + name, {defaultValue: name}) }}

  | {{#if selected_by_search_request}}
  ul.nav.nav-pills.nav-stacked: li: a(href="{{deselect_path}}")
    .fa.fa-times>
    .label.label-info
      | {{min_key}}
      =<>  t("components.HistogramFacet.to")
      | {{max_key}}

  | {{/if}}

  sparkline(class="visible-md-block visible-lg-block" style="height: 30px; width: 100%")

  .row
    .col-xs-5.col-md-5.col-lg-4
      lowerkeyinput(style="height: 28px")

    .col-xs-5.col-xs-offset-2.col-md-5.col-md-offset-2.col-lg-4.col-lg-offset-4
      upperkeyinput(style="height: 28px;")

  .row(style="margin-top: 10px")
    .col-sm-12
      slider(style="margin-left: 0.6em; margin-right: 0.6em")

  .row(style="margin-top: 10px")
    .col-sm-12
      button.btn.btn-default(style="width: 100%" on-click="update_search_result")
        = t("components.HistogramFacet.update_search_result")

| {{/facet}}
| {{/if}}
