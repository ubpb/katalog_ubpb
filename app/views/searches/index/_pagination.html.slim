.pagination
  .btn-group(role="group")
    = link_to_previous_page(scope, search_request)
    = page_stats(scope, search_request, search_result.total_hits)
    = link_to_next_page(scope, search_request, search_result.total_hits)
