= render "application/search_panel",
  scope: current_scope,
  search_request: @search_request

.search-results
  - if @search_result.present? && @search_result.hits.present? || @search_request.facet_queries.present?
    = render_relative "./index/search_result",
      notes: @notes,
      search_request: @search_request,
      search_result: @search_result,
      scope: current_scope,
      watch_lists: @watch_lists

  - else
    .panel.panel-primary
      .panel-body.panel-cutoffcorner
        = t(".no_records_found")
